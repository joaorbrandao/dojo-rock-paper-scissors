{
  "name": "rock-paper-scissors",
  "version": "1.0.0",
  "description": "Rock, Paper, Scissors game in TypeScript",
  "scripts": {
    "prebuild": "rimraf src/**/*.js test/**/*.js",
    "prestart": "npm run build",
    "pretest": "rimraf src/**/*.js test/**/*.js",
    "build": "tsc",
    "start": "node dist/src/main.js",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.11.7",
    "jest": "^27.3.1",
    "rimraf": "~3.0.2",
    "source-map-support": "^0.5.20",
    "ts-jest": "^27.0.7",
    "ts-node": "^10.4.0",
    "tsconfig-paths": "^3.11.0",
    "typescript": "^4.4.4"
  }
}
